# Algo MAX

Algo MAX — платформа мотивации учеников внутри MAX. Она объединяет астрокоины, магазин наград, заказы, склады и рабочие инструменты сотрудников в одном mini-app.

## Возможности

- личный кабинет ученика и семейный доступ для родителя;
- начисление и история движения астрокоинов;
- банковский вклад с ежедневным расчётом процентов и ежемесячной капитализацией;
- каталог наград, избранное, корзина и оформление заказов;
- управление товарами, остатками, складами и этапами выдачи;
- кабинеты преподавателя, куратора, администратора и директора;
- группы преподавателей, карточки учеников, QR-коды и ссылки-приглашения;
- импорт учеников и товаров из XLSX;
- рассылки, уведомления и отчёты по операциям;
- работа нескольких городов и партнёров с изоляцией данных.

## Как устроено

Серверная часть написана на Python и FastAPI. Данные хранятся через SQLAlchemy в PostgreSQL, структура базы управляется миграциями Alembic. Redis используется для распределённых ограничений запросов и фоновых задач.

MAX-бот отвечает за вход, привязку пользователей, уведомления и переходы в mini-app. Клиентская часть mini-app реализована на HTML, CSS и JavaScript и использует ролевой API: набор экранов и разрешённых действий формируется по назначениям пользователя в выбранном городе.

Авторизация mini-app проверяет подписанные данные запуска MAX. Доступ к ученикам, заказам, складам и отчётам дополнительно ограничивается ролью и текущим tenant. Операции с балансом, банковским вкладом, остатками и заказами выполняются транзакционно и сохраняются в истории.

Фоновые процессы обрабатывают начисления ко дню рождения, банковские проценты, уведомления и резервное копирование. Интеграционный слой поддерживает загрузку данных из CRM/LMS-выгрузок и синхронизацию рабочих данных с внешними сервисами.
